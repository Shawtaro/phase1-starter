USLOSS Phase 1

Note that phase1a.c has some errors in implementation. For other phases rely on phase1a library:

make sure the resulting file is named libphase1a-1.0.a.
Create the directory ~/cs452/lib and put the library into it. 
Typing "make" in phase1b should link against this library.
